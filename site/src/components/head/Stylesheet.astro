---
import type { Layout } from '@libs/layout'
import { getChassisCssProps } from '@libs/chassis'

interface Props {
  direction?: 'rtl'
  layout: Layout
}

const { direction } = Astro.props
---
<link {...getChassisCssProps(direction)} />
<link rel="stylesheet" href={`assets/icons/chassis-icons.min.css`} />
